#product-version: "2.9.7"
#pivnet-product-slug: ops-manager
#pivnet-file-glob: "*.ova"
#    download-stemcell: "false"

products:
  opsman:
#    product-version: "2.9.1"
#    pivnet-product-slug: ops-manager
#    pivnet-file-glob: "*.ova"
    #    download-stemcell: "false"
    s3-endpoint: ((s3_endpoint))
    s3-region-name: ((s3_region))
    s3-bucket: ((s3_bucket))
    s3-disable-ssl: "true"
    s3-access-key-id: ((s3_access_key_id))
    s3-secret-access-key: ((s3_secret_access_key))
    pivnet-api-token: ((pivnet_token))

  pks:
    product-version: "1.7.0"
    pivnet-product-slug: pivotal-container-service
    pivnet-file-glob: "pivotal-container-service*.pivotal"
    stemcell-iaas: vsphere
    s3-endpoint: ((s3_endpoint))
    s3-region-name: ((s3_region))
    s3-bucket: ((s3_bucket))
    s3-disable-ssl: "true"
    s3-access-key-id: ((s3_access_key_id))
    s3-secret-access-key: ((s3_secret_access_key))
    pivnet-api-token: ((pivnet_token))

  healthwatch2:
    product-version: "2.0.2-build.83"
    pivnet-product-slug: p-healthwatch
    pivnet-file-glob: "healthwatch-2.0.2-*.pivotal"
    stemcell-iaas: vsphere
    s3-endpoint: ((s3_endpoint))
    s3-region-name: ((s3_region))
    s3-bucket: ((s3_bucket))
    s3-disable-ssl: "true"
    s3-access-key-id: ((s3_access_key_id))
    s3-secret-access-key: ((s3_secret_access_key))
    pivnet-api-token: ((pivnet_token))